# Backend and Frontend Ports
BACKEND_PORT=8080
FRONTEND_PORT=3030

# MLflow / DagsHub Configuration
# Replace with your actual DagsHub MLflow tracking URI
# Example: https://dagshub.com/<username>/<repo>.mlflow
MLFLOW_TRACKING_URI=https://dagshub.com/<YOUR_USERNAME>/<YOUR_REPO>.mlflow

# Model URIs - Direct URIs to specific model versions
# Replace with your actual model URIs from DagsHub MLflow
# Format: models:/<ModelName>@<alias> or models:/<ModelName>/<version>
# Example: models:/Best_Production_Model@production
MODEL_URI_PROD=models:/Best_Production_Model@production
MODEL_URI_DEV=models:/Latest_Model@latest

# Optional: Backup model URI
MODEL_URI_BACKUP=models:/Backup_Model@backup

# Alternative: Model names for registry lookup (fallback if direct URIs not used)
MLFLOW_BEST_MODEL_NAME=Best_Production_Model
MLFLOW_LATEST_MODEL_NAME=Latest_Model

# DagsHub Authentication (if required)
# Uncomment and set these if your MLflow registry requires authentication
# MLFLOW_TRACKING_USERNAME=<YOUR_DAGSHUB_USERNAME>
# MLFLOW_TRACKING_PASSWORD=<YOUR_DAGSHUB_TOKEN>

# OR use environment-specific credentials
# DAGSHUB_USER_TOKEN=<YOUR_DAGSHUB_TOKEN>
